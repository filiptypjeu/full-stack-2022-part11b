{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../full-stack/part2/phonebook && npm run build && cp -r build ../../../full-stack-part3/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "dev": "nodemon src/index.js",
    "logs": "heroku logs --tail",
    "open": "npx heroku open",
    "start": "node src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.6",
    "morgan": "^1.10.0",
    "static": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
